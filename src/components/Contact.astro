---
const contactInfo = [
  {
    icon: 'mail',
    label: 'Wyślij nam wiadomość',
    value: 'technical-support@wb-partners.pl',
    href: 'mailto:technical-support@wb-partners.pl',
  },
  {
    icon: 'map',
    label: 'Odwiedź nas',
    value: 'Słowackiego 24/11 ',
    subvalue: 'Rzeszów 35-060',
    href: null,
  },
];
---

<section id="contact" class="py-24 relative overflow-hidden section-divider section-accent">
  <div class="max-w-[1280px] mx-auto px-4 sm:px-6 lg:px-8 relative">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 lg:gap-24">
      <!-- Left Column - Info -->
      <div>
        <span class="text-[#FF5E00] font-bold tracking-wider uppercase text-sm mb-4 block">Skontaktuj się z nami</span>
        <h2 class="text-4xl md:text-5xl font-bold mb-6 tracking-tight text-white">Zbudujmy coś wspaniałego razem</h2>
        <p class="text-gray-400 text-lg mb-10 leading-relaxed">
          Współpracujemy z przedsiębiorstwami o ambicjach, aby budować skalowalne, wydajne aplikacje React. Opowiedz nam o swoim projekcie.
        </p>

        <!-- Contact Info -->
        <div class="space-y-8">
          {contactInfo.map((item) => (
            <div class="flex gap-5">
              <div class="w-12 h-12 rounded-2xl bg-surface-dark flex items-center justify-center text-white shrink-0">
                <span class="material-symbols-outlined">{item.icon}</span>
              </div>
              <div>
                <h3 class="text-lg font-bold text-white mb-1">{item.label}</h3>
                {item.href ? (
                  <a href={item.href} class="text-gray-400 hover:text-[#FF5E00] transition-colors">
                    {item.value}
                  </a>
                ) : (
                  <>
                    <p class="text-gray-400">{item.value}</p>
                    {item.subvalue && <p class="text-gray-400">{item.subvalue}</p>}
                  </>
                )}
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Right Column - Form -->
      <div class="bg-surface-dark rounded-[2rem] p-8 md:p-10 border border-white/5 relative overflow-hidden">
        <div class="absolute top-0 right-0 w-32 h-32 bg-[#FF5E00]/5 rounded-full blur-3xl -translate-y-1/2 translate-x-1/2"></div>
        
        <form id="contact-form" class="space-y-5 relative z-10">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
            <!-- First Name -->
            <div class="space-y-2">
              <label class="text-sm font-bold text-gray-300 ml-1">Imię</label>
              <input 
                type="text" 
                name="firstName"
                required
                class="w-full h-12 px-4 rounded-xl bg-background-dark border border-white/10 focus:border-[#FF5E00] focus:ring-1 focus:ring-[#FF5E00] outline-none transition-all text-white placeholder:text-slate-400" 
                placeholder="Jan" 
              />
            </div>
            <!-- Last Name -->
            <div class="space-y-2">
              <label class="text-sm font-bold text-gray-300 ml-1">Nazwisko</label>
              <input 
                type="text" 
                name="lastName"
                required
                class="w-full h-12 px-4 rounded-xl bg-background-dark border border-white/10 focus:border-[#FF5E00] focus:ring-1 focus:ring-[#FF5E00] outline-none transition-all text-white placeholder:text-slate-400" 
                placeholder="Kowalski" 
              />
            </div>
          </div>

          <!-- Email -->
          <div class="space-y-2">
            <label class="text-sm font-bold text-gray-300 ml-1">Email zawodowy</label>
            <input 
              type="email" 
              name="email"
              required
              class="w-full h-12 px-4 rounded-xl bg-background-dark border border-white/10 focus:border-[#FF5E00] focus:ring-1 focus:ring-[#FF5E00] outline-none transition-all text-white placeholder:text-slate-400" 
              placeholder="jan@firma.pl" 
            />
          </div>

          <!-- Message -->
          <div class="space-y-2">
            <label class="text-sm font-bold text-gray-300 ml-1">Szczegóły projektu</label>
            <textarea 
              name="message"
              required
<<<<<<< HEAD
              rows="4"
              class="w-full p-4 rounded-xl bg-background-dark border border-white/10 focus:border-[#FF5E00] focus:ring-1 focus:ring-[#FF5E00] outline-none transition-all text-white placeholder:text-slate-400 resize-none" 
=======
              rows="5"
              class="w-full py-5 px-6 rounded-xl bg-background-dark border border-white/10 focus:border-accent focus:ring-1 focus:ring-accent outline-none transition-all text-white placeholder:text-slate-400 resize-none leading-relaxed text-base min-h-[120px]" 
>>>>>>> 251f131 (Poprawka formularza kontaktowego i aktualizacja tytułu strony)
              placeholder="Powiedz nam o swoich celach, harmonogramie i budżecie..."
            ></textarea>
          </div>

          <!-- Submit Button -->
          <div class="pt-2">
            <button 
              type="submit" 
              class="w-full h-14 rounded-full bg-[#FF5E00] hover:bg-[#e65500] text-white font-bold text-lg shadow-lg shadow-[#FF5E00]/20 hover:shadow-[#FF5E00]/40 transition-all flex items-center justify-center gap-2"
            >
              Wyślij wiadomość
              <span class="material-symbols-outlined text-xl">arrow_forward</span>
            </button>
          </div>

          <!-- Form Status -->
          <div id="form-status" class="hidden mt-4">
            <div id="form-success" class="hidden p-4 bg-primary/10 border border-primary/30 rounded-lg text-primary text-sm">
              Dziękuję! Twoja wiadomość została wysłana. Wkrótce się z Tobą skontaktujemy.
            </div>
            <div id="form-error" class="hidden p-4 bg-red-500/10 border border-red-500/30 rounded-lg text-red-400 text-sm">
              Coś poszło nie tak. Spróbuj ponownie lub wyślij nam wiadomość bezpośrednio.
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const formStatus = document.getElementById('form-status');
  const formSuccess = document.getElementById('form-success');
  const formError = document.getElementById('form-error');

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const submitBtn = form.querySelector('button[type="submit"]') as HTMLButtonElement;
    const originalText = submitBtn.innerHTML;
    
    submitBtn.disabled = true;
    submitBtn.innerHTML = `
      <svg class="animate-spin w-5 h-5" fill="none" viewBox="0 0 24 24">
        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
      </svg>
      <span>Wysyłanie...</span>
    `;

    try {
      const formData = new FormData(form);
      const data = {
        name: `${formData.get('firstName')} ${formData.get('lastName')}`,
        email: formData.get('email'),
        message: formData.get('message'),
      };

      const response = await fetch('/api/contact', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });

      if (response.ok) {
        formStatus?.classList.remove('hidden');
        formSuccess?.classList.remove('hidden');
        formError?.classList.add('hidden');
        form.reset();
      } else {
        throw new Error('Failed to send');
      }
    } catch (error) {
      formStatus?.classList.remove('hidden');
      formError?.classList.remove('hidden');
      formSuccess?.classList.add('hidden');
    } finally {
      submitBtn.disabled = false;
      submitBtn.innerHTML = originalText;
    }
  });
</script>
