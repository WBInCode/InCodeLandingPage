---
const technologies = [
  { icon: 'code', name: 'Next.js 14' },
  { icon: 'data_object', name: 'TypeScript' },
  { icon: 'brush', name: 'Tailwind CSS' },
  { icon: 'cloud', name: 'Vercel' },
];
---

<section class="relative pt-20 pb-20 md:pt-32 md:pb-32 overflow-hidden">
  <div class="relative max-w-[1280px] mx-auto px-4 sm:px-6 lg:px-8 flex flex-col items-center text-center">
    <!-- Badge -->
    <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-primary/30 bg-primary/10 text-primary text-xs font-bold uppercase tracking-wider mb-6">
      <span class="w-2 h-2 rounded-full bg-primary animate-pulse"></span>
      Przyjmujemy nowe projekty
    </div>

    <!-- Main Heading -->
    <h1 class="max-w-4xl text-5xl md:text-7xl font-bold tracking-tight mb-6 leading-tight">
      <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary to-emerald-400">WB InCode</span>
      <br class="hidden md:block" />
      <span class="text-white">Rozwiązania, które napędzają biznes</span>
    </h1>

    <!-- Subheading -->
    <p class="max-w-2xl text-lg md:text-xl text-gray-400 mb-10 leading-relaxed">
      Tworzymy wydajne strony internetowe i aplikacje webowe z wykorzystaniem najnowszych technologii React i Next.js. Specjalizujemy się w projektach wymagających wysokiej jakości kodu, świetnego UX i skalowalnej architektury – od stron firmowych po złożone platformy biznesowe.
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 w-full sm:w-auto">
      <button class="h-12 px-8 rounded-full bg-primary text-background-dark font-bold text-base hover:brightness-110 transition-all shadow-[0_0_20px_rgba(48,232,122,0.3)]">
        Rozpocznij swój projekt
      </button>
      <a href="/#work" class="h-12 px-8 rounded-full border border-white/20 hover:bg-white/5 text-white font-medium text-base transition-all flex items-center justify-center gap-2">
        <span>Zobacz nasze prace</span>
        <span class="material-symbols-outlined text-sm">arrow_forward</span>
      </a>
    </div>

    <!-- Technology Tags -->
    <div class="mt-16 flex flex-wrap justify-center gap-3 opacity-80">
      {technologies.map((tech) => (
        <div class="flex items-center gap-2 px-4 py-2 rounded-full bg-white/5 border border-white/10 shadow-sm text-white">
          <span class="material-symbols-outlined text-base">{tech.icon}</span>
          <span class="text-sm font-medium">{tech.name}</span>
        </div>
      ))}
    </div>
  </div>
</section>
