---
const projects = [
  {
    title: 'Etui App',
    category: '',
    description: 'Aplikacja do personalizacji i zamawiania etui na telefon.',
    tags: ['eCommerce'],
    gradient: 'from-violet-900 to-slate-800',
    link: 'https://etui-app.vercel.app/',
    hasPreview: true,
  },
  {
    title: 'NeoBank Dashboard',
    category: 'FINTECH',
    description: 'Platforma wizualizacji danych finansowych w czasie rzeczywistym.',
    tags: ['Next.js', 'Finanse'],
    gradient: 'from-slate-800 to-slate-600',
  },
  {
    title: 'HealthStream App',
    category: 'MEDTECH',
    description: 'System monitorowania pacjentów dla szpitali.',
    tags: ['React Native', 'Opieka zdrowotna'],
    gradient: 'from-emerald-900 to-slate-900',
  },
  {
    title: 'RetailPOS Platform',
    category: 'RETAIL',
    description: 'Moduł sprzedażowy i zarządzanie zapasami dla sieci sklepów.',
    tags: ['Next.js', 'eCommerce'],
    gradient: 'from-slate-700 to-emerald-900',
  },
  {
    title: 'Learnly LMS',
    category: 'EDUTECH',
    description: 'Platforma e-learningowa z analityką postępów.',
    tags: ['TypeScript', 'SaaS'],
    gradient: 'from-emerald-800 to-slate-800',
  },
  {
    title: 'LogiTrack',
    category: 'LOGISTICS',
    description: 'System śledzenia przesyłek z mapami i powiadomieniami.',
    tags: ['React', 'Maps'],
    gradient: 'from-slate-900 to-slate-700',
  },
];
---

<section id="work" class="py-24 section-divider">
  <div class="max-w-[1200px] mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Main container -->
    <div class="relative rounded-[2.5rem] bg-white/[0.02] backdrop-blur-sm border border-white/[0.06] p-8 md:p-12 lg:p-16 overflow-hidden">
      <!-- Decorative elements -->
      <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[400px] h-[200px] bg-primary/10 rounded-full blur-[100px]"></div>
      
      <!-- Corner accents -->
      <div class="absolute top-6 left-6 w-16 h-16 border-l-2 border-t-2 border-primary/20 rounded-tl-2xl"></div>
      <div class="absolute bottom-6 right-6 w-16 h-16 border-r-2 border-b-2 border-primary/20 rounded-br-2xl"></div>

      <!-- Section Header -->
      <div class="relative text-center mb-12">
        <span class="inline-flex items-center gap-2 text-primary font-bold tracking-wider uppercase text-xs mb-4">
          <span class="w-8 h-px bg-primary"></span>
          Portfolio
          <span class="w-8 h-px bg-primary"></span>
        </span>
        <h2 class="text-3xl md:text-5xl font-bold mb-4 text-white">Wybrane prace</h2>
        <p class="text-gray-400 max-w-2xl mx-auto">Od pomysłu do sukcesu – nasze realizacje.</p>
      </div>

      <!-- Projects Grid -->
      <div class="relative grid grid-cols-1 md:grid-cols-2 gap-6 lg:gap-8">
        {projects.map((project, index) => {
          const ProjectWrapper = project.link ? 'a' : 'div';
          const wrapperProps = project.link ? { href: project.link, target: '_blank', rel: 'noopener noreferrer' } : {};
          return (
            <a 
              href={project.link || '#'} 
              target={project.link ? '_blank' : '_self'}
              rel={project.link ? 'noopener noreferrer' : undefined}
              class={`group cursor-pointer block ${index >= 2 ? 'extra-project collapsible' : ''} ${!project.link ? 'pointer-events-none' : ''}`} 
              data-index={index}
            >
              <div class="overflow-hidden rounded-2xl mb-5 bg-surface-dark aspect-video relative border border-white/5 group-hover:border-primary/30 transition-all duration-500">
                
                <!-- Mini preview for Etui App - full bleed -->
                {project.hasPreview ? (
                  <div class="absolute inset-0 bg-[#0a0a0a] flex flex-col">
                    <!-- Top bar with logo -->
                    <div class="px-4 py-2 flex items-center justify-between border-b border-white/5">
                      <div class="flex items-center gap-1.5">
                        <div class="w-4 h-4 rounded border border-white/20 flex items-center justify-center">
                          <span class="text-[7px] text-white/60">C</span>
                        </div>
                        <span class="text-[11px] text-white font-semibold">CaseStudio.</span>
                      </div>
                      <div class="flex gap-2.5">
                        <span class="text-[8px] text-white/40">Zaloguj się</span>
                        <span class="text-[8px] text-white/40">↗</span>
                      </div>
                    </div>
                    <!-- Navigation -->
                    <div class="px-4 py-1.5 flex items-center justify-center gap-4 border-b border-white/5">
                      <span class="text-[8px] text-white/50">Zaprojektuj etui</span>
                      <span class="text-[8px] text-white/50">Gotowe etui</span>
                      <span class="text-[8px] text-white/50">Szkła</span>
                      <span class="text-[8px] text-emerald-400">Nowości</span>
                      <span class="text-[8px] text-orange-400">Bestsellery 🔥</span>
                    </div>
                    <!-- Promo banner -->
                    <div class="py-1.5 text-center border-b border-white/5">
                      <span class="text-[8px] text-white/60">🎄 Świąteczna promocja! <span class="text-emerald-400">-20%</span> z kodem: XMAS2025</span>
                    </div>
                    <!-- Hero section -->
                    <div class="flex-1 flex flex-col items-center justify-center px-4 py-3">
                      <div class="text-xl md:text-2xl lg:text-3xl text-white font-black italic leading-tight text-center">Zaprojektuj</div>
                      <div class="text-xl md:text-2xl lg:text-3xl font-black italic leading-tight text-center"><span class="text-white">Swoje </span><span class="text-gray-500">Etui</span></div>
                      <div class="text-[8px] md:text-[9px] text-white/40 mt-2 max-w-[280px] mx-auto text-center">Poznaj nową generację personalizacji. Stwórz produkty idealnie dopasowane.</div>
                      <div class="mt-3 inline-flex items-center gap-1.5 bg-white text-black text-[9px] font-medium px-4 py-1.5 rounded-full">
                        <span>⟡</span> Rozpocznij projektowanie <span>→</span>
                      </div>
                    </div>
                    <!-- Feature cards -->
                    <div class="px-4 pb-4 flex justify-center gap-3">
                      <div class="flex-1 max-w-[120px] bg-white/5 rounded-xl p-3 border border-white/10">
                        <div class="text-[8px] text-white/80 font-medium">Materiały premium</div>
                        <div class="text-[7px] text-white/30 mt-0.5">Skóra, zamsz...</div>
                      </div>
                      <div class="flex-1 max-w-[120px] bg-white/5 rounded-xl p-3 border border-white/10">
                        <div class="w-full h-8 bg-gradient-to-br from-slate-600 to-slate-800 rounded-lg mb-1.5"></div>
                        <div class="text-[8px] text-white/80 font-medium">Wizualizacja 3D</div>
                      </div>
                      <div class="flex-1 max-w-[120px] bg-white/5 rounded-xl p-3 border border-white/10">
                        <div class="text-[8px] text-white/80 font-medium">Szybka dostawa</div>
                        <div class="text-[7px] text-white/30 mt-0.5">Na cały świat</div>
                      </div>
                    </div>
                    <!-- Hover overlay -->
                    <div class="absolute inset-0 bg-primary/0 group-hover:bg-primary/5 transition-colors duration-500 pointer-events-none"></div>
                  </div>
                ) : (
                  <>
                    <div class={`absolute inset-0 bg-gradient-to-tr ${project.gradient} opacity-90 group-hover:scale-110 transition-transform duration-700`}></div>
                    <!-- Category watermark -->
                    <div class="absolute inset-0 flex items-center justify-center">
                      <span class="text-white/10 text-5xl md:text-6xl font-black tracking-tighter select-none">{project.category}</span>
                    </div>
                    <!-- Hover overlay -->
                    <div class="absolute inset-0 bg-primary/0 group-hover:bg-primary/10 transition-colors duration-500"></div>
                  </>
                )}
                
                <!-- Tags at bottom -->
                <div class="absolute bottom-0 left-0 p-5 w-full bg-gradient-to-t from-black/90 via-black/50 to-transparent">
                  <div class="flex gap-2">
                    {project.tags.map((tag) => (
                      <span class="text-xs font-bold px-3 py-1.5 bg-white/10 backdrop-blur-sm rounded-full text-white border border-white/10">{tag}</span>
                    ))}
                  </div>
                </div>
                
                <!-- View project indicator -->
                {project.link && (
                  <div class="absolute top-4 right-4 w-10 h-10 rounded-full bg-white/10 backdrop-blur-sm flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-300 border border-white/20">
                    <span class="material-symbols-outlined text-white text-lg">arrow_outward</span>
                  </div>
                )}
              </div>
              
              <h3 class="text-xl md:text-2xl font-bold mb-2 text-white group-hover:text-primary transition-colors">{project.title}</h3>
              <p class="text-gray-500 text-sm">{project.description}</p>
            </a>
          );
        })}
      </div>

      <!-- View All Link -->
          <div class="relative mt-12 text-center">
            <button id="view-all-projects" class="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-white/5 border border-white/10 text-sm font-bold uppercase tracking-wider text-white hover:text-primary hover:border-primary/30 hover:bg-primary/5 transition-all duration-300">
              <span id="view-all-text">Pokaż wszystkie case studies</span>
              <span id="view-all-icon" class="material-symbols-outlined text-lg">arrow_forward</span>
            </button>
          </div>
    </div>
  </div>
</section>

<script>
  const btn = document.getElementById('view-all-projects');
  const extras = Array.from(document.querySelectorAll('.extra-project'));
  const text = document.getElementById('view-all-text');
  const icon = document.getElementById('view-all-icon');
  let open = false;

  btn?.addEventListener('click', (e) => {
    e.preventDefault();
    open = !open;
    extras.forEach((el, i) => {
      // add slight stagger for nicer effect
      setTimeout(() => el.classList.toggle('open', open), i * 50);
    });
    text.innerText = open ? 'Pokaż mniej' : 'Pokaż wszystkie case studies';
    icon.style.transform = open ? 'rotate(180deg)' : 'rotate(0deg)';
  });
</script>
